创建软连接：
	ln -s 原始文件或目录 目标路径及名称
	#软连接的意思就是创建快捷方式，如果原始文件或目录被删除了，则快捷方式失效，软连接可存放在和原始文件或目录不同的分区；不同的文件系统下，且支持给目录做快捷方式
创建硬连接：
	ln 原始文件 目标路径及名称
	#区别与软连接，虽然也是做快捷方式，但是原始文件被删除，这个快捷方式仍然可用，但是要求，必须要与原文件在同一分区，同一个文件系统才行，且不支持给目录做快捷方式









